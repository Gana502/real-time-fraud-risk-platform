apiVersion: v1
kind: Service
metadata:
  name: fraud-risk-api-service
spec:
  type: NodePort
  selector:
    app: fraud-risk-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080